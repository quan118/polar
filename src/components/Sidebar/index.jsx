import { memo } from "react";

const Sidebar = () => {
  return (
    <div className="flex-column flex w-16 justify-center bg-indigo-500">
      Sidebar TODO
    </div>
  );
};

export default memo(Sidebar);
